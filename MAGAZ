<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Магазин странных вещей</title>

<style>
    * {
        box-sizing: border-box;
    }

    body {
        margin: 0;
        font-family: Arial, sans-serif;
        background: linear-gradient(135deg, #24104f, #111827);
        color: white;
        min-height: 100vh;
        overflow-x: hidden;
    }

    header {
        text-align: center;
        padding: 20px 10px 10px;
    }

    h1 {
        margin: 0;
        font-size: 32px;
    }

    .subtitle {
        color: #c4b5fd;
        margin-top: 5px;
    }

    .stats {
        display: flex;
        justify-content: center;
        gap: 15px;
        flex-wrap: wrap;
        margin: 15px;
    }

    .stat {
        background: rgba(255,255,255,0.1);
        border: 1px solid rgba(255,255,255,0.15);
        padding: 10px 18px;
        border-radius: 15px;
    }

    .shop {
        max-width: 1000px;
        margin: auto;
        padding: 10px;
    }

    .customer {
        background: rgba(255,255,255,0.1);
        border-radius: 25px;
        padding: 20px;
        text-align: center;
        margin-bottom: 20px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.25);
    }

    .customer-face {
        font-size: 70px;
    }

    .request {
        font-size: 23px;
        font-weight: bold;
        margin: 10px 0;
    }

    .timer {
        color: #fbbf24;
        font-size: 18px;
    }

    .items {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
        gap: 12px;
    }

    .item {
        background: #312e81;
        border: 2px solid transparent;
        border-radius: 18px;
        padding: 15px;
        cursor: pointer;
        transition: 0.15s;
        user-select: none;
    }

    .item:hover {
        transform: translateY(-4px);
        border-color: #a78bfa;
        background: #4338ca;
    }

    .item:active {
        transform: scale(0.95);
    }

    .emoji {
        font-size: 42px;
    }

    .item-name {
        font-weight: bold;
        margin-top: 8px;
    }

    .price {
        color: #fcd34d;
        margin-top: 5px;
    }

    .message {
        min-height: 30px;
        text-align: center;
        font-size: 18px;
        margin: 15px;
        font-weight: bold;
    }

    .success {
        color: #4ade80;
    }

    .error {
        color: #fb7185;
    }

    button {
        border: none;
        border-radius: 15px;
        padding: 12px 20px;
        font-size: 16px;
        cursor: pointer;
        background: #8b5cf6;
        color: white;
        font-weight: bold;
    }

    button:hover {
        background: #7c3aed;
    }

    .upgrades {
        margin-top: 25px;
        background: rgba(255,255,255,0.08);
        padding: 20px;
        border-radius: 20px;
    }

    .upgrades h2 {
        text-align: center;
    }

    .upgrade {
        background: rgba(0,0,0,0.2);
        padding: 15px;
        border-radius: 15px;
        margin-top: 10px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 10px;
    }

    .upgrade-info {
        flex: 1;
    }

    .small {
        color: #c4b5fd;
        font-size: 13px;
    }

    .floating {
        position: fixed;
        pointer-events: none;
        font-weight: bold;
        animation: floatUp 1s forwards;
    }

    @keyframes floatUp {
        from {
            opacity: 1;
            transform: translateY(0);
        }
        to {
            opacity: 0;
            transform: translateY(-80px);
        }
    }

    @media(max-width: 500px) {
        h1 {
            font-size: 25px;
        }

        .request {
            font-size: 19px;
        }
    }
</style>
</head>

<body>

<header>
    <h1>🛒 МАГАЗИН СТРАННЫХ ВЕЩЕЙ</h1>
    <div class="subtitle">
        Здесь можно купить абсолютно всё. Зачем? Никто не знает.
    </div>
</header>

<div class="stats">
    <div class="stat">💰 Деньги: <b id="money">100</b></div>
    <div class="stat">⭐ Репутация: <b id="reputation">0</b></div>
    <div class="stat">📦 Продано: <b id="sold">0</b></div>
</div>

<div class="shop">

    <div class="customer">
        <div class="customer-face" id="customerFace">🧔</div>

        <div>
            Посетитель говорит:
        </div>

        <div class="request" id="request">
            Загрузка заказа...
        </div>

        <div class="timer">
            ⏱️ Осталось: <span id="timer">20</span> сек.
        </div>
    </div>

    <div class="message" id="message"></div>

    <div class="items" id="items"></div>

    <div class="upgrades">
        <h2>🔧 Улучшения магазина</h2>

        <div class="upgrade">
            <div class="upgrade-info">
                <b>🏪 Больше полок</b>
                <div class="small">
                    Увеличивает количество доступных предметов.
                </div>
            </div>

            <button onclick="buyShelf()">
                💰 <span id="shelfPrice">200</span>
            </button>
        </div>

        <div class="upgrade">
            <div class="upgrade-info">
                <b>✨ Страннее товары</b>
                <div class="small">
                    Открывает ещё более безумные предметы.
                </div>
            </div>

            <button onclick="buyWeirdness()">
                💰 <span id="weirdPrice">300</span>
            </button>
        </div>

        <div class="upgrade">
            <div class="upgrade-info">
                <b>📢 Реклама</b>
                <div class="small">
                    Посетители начинают платить больше.
                </div>
            </div>

            <button onclick="buyAds()">
                💰 <span id="adsPrice">500</span>
            </button>
        </div>
    </div>

</div>

<script>

const normalItems = [
    {name:"Носок", emoji:"🧦", price:25},
    {name:"Серебряный стул", emoji:"🪑", price:80},
    {name:"Золотая ложка", emoji:"🥄", price:120},
    {name:"Кирпич", emoji:"🧱", price:35},
    {name:"Резиновая уточка", emoji:"🦆", price:45},
    {name:"Огромная картошка", emoji:"🥔", price:60},
    {name:"Пластиковая корона", emoji:"👑", price:70},
    {name:"Камень", emoji:"🪨", price:15},
    {name:"Банка воздуха", emoji:"🫙", price:55},
    {name:"Один карандаш", emoji:"✏️", price:20}
];

const weirdItems = [
    {name:"Невидимый кирпич", emoji:"👻", price:250},
    {name:"Ложка размером с человека", emoji:"🥄", price:350},
    {name:"Золотой носок", emoji:"🧦", price:400},
    {name:"Кубик льда из будущего", emoji:"🧊", price:500},
    {name:"Карманная дверь", emoji:"🚪", price:600},
    {name:"Очень подозрительная картошка", emoji:"🥔", price:750},
    {name:"Микроволновка для камней", emoji:"📻", price:900},
    {name:"Колесо без машины", emoji:"🛞", price:300}
];

const faces = [
    "🧔",
    "👨‍🦰",
    "👩",
    "👨",
    "🧑",
    "👴",
    "👵",
    "🤠",
    "🥸",
    "🤡"
];

let money = 100;
let reputation = 0;
let sold = 0;

let shelfLevel = 0;
let weirdness = 0;
let ads = 0;

let currentRequest;
let timeLeft = 20;
let timerInterval;

const moneyEl = document.getElementById("money");
const reputationEl = document.getElementById("reputation");
const soldEl = document.getElementById("sold");

const requestEl = document.getElementById("request");
const timerEl = document.getElementById("timer");
const itemsEl = document.getElementById("items");
const messageEl = document.getElementById("message");
const customerFace = document.getElementById("customerFace");

function randomItem() {

    let pool = [...normalItems];

    if (weirdness > 0) {
        pool = [...pool, ...weirdItems.slice(0, weirdness * 3)];
    }

    return pool[Math.floor(Math.random() * pool.length)];
}

function newCustomer() {

    clearInterval(timerInterval);

    currentRequest = randomItem();

    requestEl.innerText =
        `«Мне нужен ${currentRequest.name.toLowerCase()}!»`;

    customerFace.innerText =
        faces[Math.floor(Math.random() * faces.length)];

    timeLeft = 20;

    timerEl.innerText = timeLeft;

    timerInterval = setInterval(() => {

        timeLeft--;

        timerEl.innerText = timeLeft;

        if (timeLeft <= 0) {

            clearInterval(timerInterval);

            messageEl.innerHTML =
                `<span class="error">
                😡 Клиент ушёл! Он очень расстроен.
                </span>`;

            setTimeout(newCustomer, 1000);
        }

    }, 1000);
}

function renderItems() {

    itemsEl.innerHTML = "";

    let pool = [...normalItems];

    if (weirdness > 0) {
        pool = [...pool, ...weirdItems.slice(0, weirdness * 3)];
    }

    pool.forEach(item => {

        const div = document.createElement("div");

        div.className = "item";

        div.innerHTML = `
            <div class="emoji">${item.emoji}</div>
            <div class="item-name">${item.name}</div>
            <div class="price">💰 ${item.price}</div>
        `;

        div.onclick = () => sellItem(item);

        itemsEl.appendChild(div);
    });
}

function sellItem(item) {

    if (timeLeft <= 0) return;

    if (item.name === currentRequest.name) {

        let reward = item.price;

        reward += ads * 25;

        money += reward;
        reputation++;
        sold++;

        messageEl.innerHTML =
            `<span class="success">
            ✅ Продано! Клиент счастлив. +${reward}💰
            </span>`;

        updateStats();

        clearInterval(timerInterval);

        createFloating(`+${reward}💰`);

        setTimeout(newCustomer, 900);

    } else {

        messageEl.innerHTML =
            `<span class="error">
            ❌ Нет! Клиент просил "${currentRequest.name}"!
            </span>`;
    }
}

function updateStats() {

    moneyEl.innerText = money;
    reputationEl.innerText = reputation;
    soldEl.innerText = sold;

    document.getElementById("shelfPrice").innerText =
        200 * Math.pow(2, shelfLevel);

    document.getElementById("weirdPrice").innerText =
        300 * Math.pow(2, weirdness);

    document.getElementById("adsPrice").innerText =
        500 * Math.pow(2, ads);
}

function buyShelf() {

    const price = 200 * Math.pow(2, shelfLevel);

    if (money < price) {

        showError("💸 Не хватает денег!");

        return;
    }

    money -= price;
    shelfLevel++;

    showSuccess("🏪 Полки расширены!");

    updateStats();
}

function buyWeirdness() {

    const price = 300 * Math.pow(2, weirdness);

    if (money < price) {

        showError("💸 Не хватает денег!");

        return;
    }

    if (weirdness >= 3) {

        showError("🤯 Товары уже настолько странные, насколько возможно!");

        return;
    }

    money -= price;
    weirdness++;

    renderItems();

    showSuccess("🤪 В магазин приехали ещё более странные товары!");

    updateStats();
}

function buyAds() {

    const price = 500 * Math.pow(2, ads);

    if (money < price) {

        showError("💸 Не хватает денег!");

        return;
    }

    money -= price;
    ads++;

    showSuccess("📢 Реклама привела странных клиентов!");

    updateStats();
}

function showError(text) {

    messageEl.innerHTML =
        `<span class="error">${text}</span>`;
}

function showSuccess(text) {

    messageEl.innerHTML =
        `<span class="success">${text}</span>`;
}

function createFloating(text) {

    const el = document.createElement("div");

    el.className = "floating";

    el.innerText = text;

    el.style.left =
        Math.random() * 80 + 10 + "%";

    el.style.top = "50%";

    document.body.appendChild(el);

    setTimeout(() => {
        el.remove();
    }, 1000);
}

renderItems();
updateStats();
newCustomer();

</script>

</body>
</html>
